# Deadly Corridor Scenario
doom_scenario_path = deadly_corridor.wad
doom_map = map01

# Recompensas y castigos
living_reward = -0.0001
death_penalty = 1.0

# Condiciones de renderizado
screen_resolution = RES_320X240
screen_format = CRCGCB
render_hud = true
render_crosshair = false
render_weapon = true
render_decals = false
render_particles = false

# Tiempo de juego 
episode_timeout = 2100

# Botones disponibles (Acciones del agente)
available_buttons = 
	{ 
		MOVE_LEFT 
		MOVE_RIGHT 
		ATTACK 
		MOVE_FORWARD
		MOVE_BACKWARD
		TURN_LEFT
		TURN_RIGHT
	}

# Variables que el agente puede observar
available_game_variables = { HEALTH ARMOR SELECTED_WEAPON_AMMO }

mode = PLAYER
doom_skill = 3